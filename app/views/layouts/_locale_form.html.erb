<%= form_for current_user.setting || current_user.build_setting, url: setting_path, html: { id: 'top_locale_selector' } do |f| %>
  <%= f.select :locale, [['EN', :en], ['ID', :id]], {}, id: 'top_user_setting_locale', direct_submit: true, class: 'form-control' %>
<% end %>