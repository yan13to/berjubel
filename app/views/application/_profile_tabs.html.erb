<ul class="nav nav-tabs" id="profileTab" role="tablist">
  <li class="nav-item" role="presentation">
    <%- if ['profiles'].include?(controller_name) && ['new', 'edit', 'show'].include?(action_name) %>
      <strong class="nav-link active"><%= User::Profile.model_name.human %></strong>
    <% else %>
      <%= link_to content_tag(:strong, User::Profile.model_name.human), profile_path, class: "nav-link" %>
    <% end %>
  </li>
  <li class="nav-item" role="presentation">
    <%- if ['accounts'].include?(controller_name) && ['edit', 'show'].include?(action_name) %>
      <strong class="nav-link active"><%= t(:account) %></strong>
    <% else %>
      <%= link_to content_tag(:strong, t(:account)), account_path, class: "nav-link" %>
    <% end %>
  </li>
</ul>
