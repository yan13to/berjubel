<%- @stores.each do |store| %>
  <li>
    <%= link_to store.name, store_path(store), class: 'dropdown-item' %>
  </li>
<% end %>
<li>
  <%= link_to t(:all), stores_path, class: 'dropdown-item' %>
</li>
